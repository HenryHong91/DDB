import React from "react";

const ChartsHeader = ({ title }) => {
  return (
    <div className="mb-10 w-full flex justify-center pl-10 text-3xl font-bold">
      {title}
    </div>
  );
};

export default ChartsHeader;
